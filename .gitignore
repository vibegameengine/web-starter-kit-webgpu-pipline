node_modules/
dist/
dist-*/
.vite/
*.log
scripts/_dev.log

# heavy / regenerable
shots/

# external reference clones (public repos, not our code)
references/spiri0-clouds/
references/TheLongSilence/
references/*.zip

# vendored donor repo keeps its own history
vendor/webgiya/
.code-graph/
public/bakes/
vendor/react-three-fiber/
vendor/three/
vendor/three-r186/
tmp/dashboard/

# ctx-kit (machine-specific paths + local index)
.claude/settings.local.json
.codex/hooks.json
.codex/hooks/ctx-kit-codex.ps1
.codex/hooks/ctx-kit-codex.sh

# Sibling git worktrees live here; they are checkouts, not sources of this one.
worktrees/
